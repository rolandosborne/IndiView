export interface DiatumContext {
  context: any;
}

export interface DiatumSession {
  amigoId: string;
  amigoRegistry: string;
  amigoNode: string;
  amigoToken: string;
  appNode: string;
  appToken: string;
}

